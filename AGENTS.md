Do not try to modify pyproject.toml because it is read-only: it is automatically
generated by ./tools/initialize.sh.

Keep CLI scripts focused on argument parsing and command orchestration. Move
functions that may be useful to other parts of the codebase into an appropriate
reusable library module instead of defining them in a CLI script.

Write always a python 3.9-compatible code. Avoid new syntax valid only for python 3.10 or newer.
