# ToDo list
Bugs:
 - `__get_symmetry_operations` with `Tensor`
 
Scripts:
 - script to visualize the difference between reference and computed BEC
 - script to visualize the convergence of the computed BEC at varying number of adopted structures
 - `prepare_linear_system.py`: disable `spglb` to use brute force finite difference

`pytest`:
 - add more structures
 - set up a general workflow to evaluate symmetries

Improvements:
 - play around with `rcond`
